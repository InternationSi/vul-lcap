{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "admin": "yarn workspace vul-admin start",
    "shed": "yarn workspace vul-shed start",
    "build": "lerna run build",
    "clean": "lerna clean"
  },
  "devDependencies": {
    "lerna": "^5.5.4"
  },
  "dependencies": {
    "@umijs/plugin-qiankun": "^2.40.0"
  }
}
