{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "admin": "yarn workspace vul-admin start",
    "build": "lerna run build",
    "clean": "lerna clean"
  },
  "devDependencies": {
    "lerna": "^5.5.4"
  }
}
